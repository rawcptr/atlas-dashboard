[project]
name = "atlas"
version = "0.1.0"
description = "connects to the atlas dashboard and provides metrics"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "websockets>=15.0.1",
]
